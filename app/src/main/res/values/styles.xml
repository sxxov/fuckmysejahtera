<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="android:letterSpacing">-0.069</item>
        <item name="snackbarStyle">@style/Snackbar</item>
        <item name="colorSurface">?attr/colourDivider</item>
        <item name="colorOnSurface">?attr/colourDivider</item>
    </style>

    <style name="FloatingActionMenu_menu_fab_show_animation">

    </style>

    <!-- Snackbar -->
    <style
        name="TextAppearance.Design.Snackbar.Message"
        parent="android:TextAppearance"
        tools:override="true">
        <item name="android:fontFamily">@font/space_mono</item>
        <item name="android:textColor">?attr/colourTextPrimary</item>
        <item name="android:gravity">center</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_width">match_parent</item>
    </style>
    <style name="Snackbar" parent="Widget.MaterialComponents.Snackbar">
        <item name="android:background">@drawable/common_background_rounded_accented</item>
        <item name="android:paddingTop">3dp</item>
        <item name="android:paddingBottom">3dp</item>
        <item name="android:paddingStart">36dp</item>
        <item name="android:paddingEnd">36dp</item>
    </style>

    <!-- Splash Screen -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <!-- Alert Dialog -->
    <style name="AlertDialog" parent="AlertDialog.AppCompat">
        <item name="android:background">@drawable/common_background_rounded</item>
    </style>

    <!-- Popup Menu -->
    <style name="PopupMenu" parent="@android:style/Widget.PopupMenu">
        <item name="android:background">?attr/colourBackground</item>
        <item name="android:textColor">?attr/colourTextPrimary</item>
    </style>

    <!-- Number Picker -->
    <style name="AppTheme.Picker" parent="AppTheme">
        <item name="android:textColorPrimary">?attr/colourTextPrimary</item>
    </style>

    <!-- Light Theme -->
    <style name="Light" parent="ActivityTheme.Primary.Base.Light">
        <item name="android:textColorPrimary">?attr/colourTextPrimary</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- Dark Theme -->
    <style name="Dark" parent="ActivityTheme.Primary.Base.Dark">
        <item name="android:textColorPrimary">?attr/colourTextPrimary</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Light base activity theme -->
    <style name="ActivityTheme.Primary.Base.Light" parent="AppTheme">
        <item name="colourTextPrimary">@color/colorPrimaryDark</item>
        <item name="colourTextSecondary">@color/colorPrimary</item>
        <item name="colourBackground">@color/colorLight</item>
        <item name="colourDivider">@color/colorLightDark</item>
        <item name="android:windowBackground">@color/colorLight</item>
        <!--<item name="iconPlaceholder">@drawable/i_light_plholder</item> -->
        <item name="colorControlNormal">@color/colorPrimaryDark</item>
    </style>

    <!-- Dark base activity theme -->
    <style name="ActivityTheme.Primary.Base.Dark"    parent="AppTheme">
        <item name="colourTextPrimary">@color/colorLight</item>
        <item name="colourTextSecondary">@color/colorLightDark</item>
        <item name="colourBackground">@color/colorPrimaryDark</item>
        <item name="colourDivider">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/colorPrimaryDark</item>
        <item name="colorControlNormal">@color/colorLight</item>
        <!--<item name="iconPlaceholder">@drawable/i_dark_pholder</item>-->
    </style>

    <!-- taller seekbar -->
    <style name="tallerBarStyle" parent="@android:style/Widget.SeekBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:minHeight">8dip</item>
        <item name="android:maxHeight">80dip</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="rounded">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">28dp</item>
    </style>
</resources>
